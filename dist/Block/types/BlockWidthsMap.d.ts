import { BlockWidth } from "./BlockWidth";
export declare type BlockWidthsMap = {
  xs?: BlockWidth;
  sm?: BlockWidth;
  md?: BlockWidth;
  lg?: BlockWidth;
  xl?: BlockWidth;
  xxl?: BlockWidth;
};
